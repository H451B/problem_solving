#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x;
    cin >> x;
    while (x--){
        int y = 0;
        string s, p, q, r;
        cin >> s;
        cout<<s<<endl;
        // map<string, vector<string>> mp;

        for (auto i : s){
            // cout<<i<<" ";
            // if (i == ' '){
            //     cout<<y<<endl;
            //     y += 1;
            //     if (y == 1){
            //         // p = r;
            //         cout<<r<<" ";
            //     }else if (y == 3){
            //         cout<<r<<" ";
            //         // q = r;
            //     }
            //     r.clear();
            // }
            // cout << r + (char)tolower(i);
            // r + (char)tolower(i);
        }
        // mp[q].push_back(p);
        // cout<<endl;

        // Access elements in the map
        // for (const auto &[key, vec] : mp){
        //     // std::cout << key << ": ";
        //     for (const auto &value : vec){
        //         std::cout << value << " ";
        //     }
        //     std::cout << std::endl;
        // }
    }
    return 0;
}