#include <bits/stdc++.h>
using namespace std;

#define CStream ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)
#define f1(n) for(int i=0;i<n;i+=1)
#define ll long long
#define pb push_back

int main(){
    // CStream;

    // ll x,y,z;
    // cin>>x>>y;
    // vector<int>v(x+1);
    // f1(x){
    //     cin>>z;
    //     v.pb(z);
    //     v[i]+=v[i-1];
    // }
    // for(auto it:v){
    //     cout<<it<<endl;
    // }
    // int n,m;
    // cin>>n>>m;
    vector<int>a(5);
    for (int k=0;k<4; k++) {
        cin>>a[k];
        a[k]+=a[k - 1];
    }
    for(auto i:a){
        cout<<i<<" ";
    }
   return 0;
}